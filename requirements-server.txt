# Requirements for running the ConvertLeica web server (server.py)
# Includes all core conversion dependencies plus server-specific packages

# Core conversion dependencies (same as requirements-docker.txt)
numpy>=2.0.0
pyvips==3.1.1
opencv-python==4.13.0.90

# Note: The web server uses only Python standard library modules:
# - http.server (SimpleHTTPRequestHandler, ThreadingHTTPServer)
# - webbrowser
# - threading
# - tempfile
# - json
# - urllib.parse
# No additional pip packages are required for server functionality.
