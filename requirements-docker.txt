# Core runtime requirements for ConvertLeica-Docker container
# These are the minimal dependencies needed for the Docker image
# See also: requirements-server.txt and requirements-qt6ui.txt for other scenarios

numpy>=2.0.0
pyvips==3.1.1
opencv-python==4.13.0.90
